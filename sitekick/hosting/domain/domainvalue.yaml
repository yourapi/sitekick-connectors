type: FloatInt

include:
    - domdetailer
    - keywords
    - whois

result: |
    << functions.hosting.domain_value(
        params.domain,
        include.whois.attributes,
        get(include.keywords.links.files['full.json']).json(),
        include.domdetailer.attributes) >>

index:
    value: << result.domainvalue_num >> -> float
    mozRank: << result.mozRank >> -> float
