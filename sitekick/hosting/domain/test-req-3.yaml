type: StrDateInt  # Type of the data-form will be determined automatically in the future

include:
    - test-req-1
    - test-req-2

requires(424): << include['test-req-1'].attributes.now or include['test-req-2'].attributes.now >>

code: |
    import time
    import arrow

index:
    now: << include['test-req-1'].attributes.now or include['test-req-2'].attributes.now >> -> datetime
    timestamp: << include['test-req-1'].attributes.timestamp or include['test-req-2'].attributes.timestamp >> -> str
